---
const assetsURL = "https://common-ethmarks.vercel.app";

const headerHTML = await fetch(`${assetsURL}/header-about.wc.html`).then((r) =>
    r.text(),
);
const footerHTML = await fetch(`${assetsURL}/footer.wc.html`).then((r) =>
    r.text(),
);

const title = "About Me";
const description = "About Me page for Ethan Marks";
const sourceURL = "https://github.com/ethmarks/about";
---

<!doctype html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width" />
        <title>{title}</title>

        <meta name="generator" content={Astro.generator} />
        <meta name="description" content={description} />
        <meta property="og:title" content={title} />
        <meta property="og:description" content={description} />
        <meta property="og:type" content="profile" />

        <link
            rel="icon"
            href={`${assetsURL}/ethmarks.ico`}
            type="image/x-icon"
        />
        <link rel="stylesheet" href={`${assetsURL}/ethmarks-rich.css`} />
        <script src={`${assetsURL}/ethmarks.wc.js`} is:inline defer></script>
    </head>
    <body>
        <eth-header active="about">
            <Fragment set:html={headerHTML} />
        </eth-header>
        <slot />
        <eth-footer source={sourceURL}>
            <Fragment set:html={footerHTML} />
        </eth-footer>
    </body>
</html>
